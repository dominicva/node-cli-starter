{
  "name": "{{name}}",
  "version": "{{version}}",
  "description": "{{description}}",
  "bin": {
    "{{name}}": "index.js",
    "{{command}}": "index.js"
  },
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": {
    "name": "{{authorName}}",
    "email": "{{authorEmail}}",
    "url": "{{authorURL}}"
  },
  "license": "{{license}}"
}
